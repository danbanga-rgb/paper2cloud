import type { ReactNode } from "react";
// Phase 1: require an active app_users row; show the bottom nav (Home · History) and the open-items badge.
export default function UploaderLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
