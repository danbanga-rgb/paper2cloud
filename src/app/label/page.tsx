import { Stub } from "@/components/Stub";
// Phase 0 labeling helper: image left, model draft JSON right, save to fixtures/labels/. Local dev only.
export default function Page() { return <Stub screen="S0" title="Fixture labeler (dev only)" phase={0} />; }
