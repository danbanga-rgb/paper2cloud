/**
 * Phase 0: score the extraction provider against fixtures/labels/*.json and print SPEC §13 metrics.
 * Skeleton — the agent implements: load manifest + labels, run extractDocument per labeled doc,
 * compare required fields (exact match after normalization), compute per-field accuracy and the
 * wrong-but-confident rate (wrong AND confidence ≥ CONFIDENCE.normal), append a row to fixtures/SCORES.md.
 */
console.error("score-fixtures: not implemented — Phase 0 task (docs/phases.md)");
process.exit(1);
